import { JoinCircleForm } from '@/features/invitations/join-form';

/** Join a care circle by invitation code (any authenticated user). */
export default function JoinCircleScreen() {
  return <JoinCircleForm />;
}
